# ----- Ingress -----
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hello
  namespace: 01-static-yaml
  labels:
    app: hello
  annotations:
    link.argocd.argoproj.io/external-link: http://hello.74.220.22.248.sslip.io
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`hello.74.220.22.248.sslip.io`)
    kind: Rule
    services:
    - name: hello
      port: 80
