# ----- Ingress -----
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hello
  labels:
    app: hello
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`yolo.74.220.22.248.sslip.io`)
    kind: Rule
    services:
    - name: hello
      port: 80
