# ----- Ingress -----
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hello
spec:
  routes:
  - match: Host(`staging.74.220.22.248.sslip.io`)
    kind: Rule
    services:
    - name: hello
      port: 80
